# com.fairhome.dedup.DuplicateResolution

**File:** `src/main/java/com/fairhome/dedup/DuplicateResolution.java`

OPEN, CONFIRMED_DUPLICATE, NOT_A_DUPLICATE.

**Call path**

- POST /api/admin/duplicates/{id}/resolve decision field

This folder is documentation only. It does not change the Java class.
