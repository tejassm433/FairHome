# com.fairhome.draw.DrawMode

**File:** `src/main/java/com/fairhome/draw/DrawMode.java`

DRY_RUN or FINAL query param on POST /api/admin/draws.

**Call path**

- mode=DRY_RUN default

This folder is documentation only. It does not change the Java class.
