# com.fairhome.config.SecurityConfig

**File:** `src/main/java/com/fairhome/config/SecurityConfig.java`

Two filter chains: /api/admin/** HTTP Basic; browser /admin form login; public API open.

**Call path**

- Request -> apiAdminSecurity OR webSecurity -> controller

This folder is documentation only. It does not change the Java class.
