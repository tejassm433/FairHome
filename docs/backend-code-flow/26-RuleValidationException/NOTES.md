# com.fairhome.rules.RuleValidationException

**File:** `src/main/java/com/fairhome/rules/RuleValidationException.java`

HTTP 422 list of problems when a rule book is illegal.

**Call path**

- RuleSetService.validate -> ApiExceptionHandler.rules

This folder is documentation only. It does not change the Java class.
