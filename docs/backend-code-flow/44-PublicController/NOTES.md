# com.fairhome.web.PublicController

**File:** `src/main/java/com/fairhome/web/PublicController.java`

HTML public pages. Status POST uses the same lookup as the API.

**Call path**

- Prefer GET /api/applications/{n}/status for Postman

This folder is documentation only. It does not change the Java class.
