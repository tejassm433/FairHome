# com.fairhome.dedup.DuplicateFlagRepository

**File:** `src/main/java/com/fairhome/dedup/DuplicateFlagRepository.java`

Open queue by score, flags for an application id, counts.

**Call path**

- openQueue / openQueue(Pageable)

This folder is documentation only. It does not change the Java class.
