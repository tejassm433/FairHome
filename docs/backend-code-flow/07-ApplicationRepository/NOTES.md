# com.fairhome.application.ApplicationRepository

**File:** `src/main/java/com/fairhome/application/ApplicationRepository.java`

Spring Data lookups by number, national ID, DOB, phone, email, status.

**Call path**

- DedupService.findCandidates loads pool via these queries

This folder is documentation only. It does not change the Java class.
