# com.fairhome.draw.AllocationEngine

**File:** `src/main/java/com/fairhome/draw/AllocationEngine.java`

Pure function: exclude, token=SHA256(seed:number), quotas, spill, waitlist, reasons.

**Call path**

- DrawService.run -> engine.run -> list of decisions

This folder is documentation only. It does not change the Java class.
