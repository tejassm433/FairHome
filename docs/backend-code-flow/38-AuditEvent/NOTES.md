# com.fairhome.audit.AuditEvent

**File:** `src/main/java/com/fairhome/audit/AuditEvent.java`

sequence, actor, action, detail, previousHash, entryHash (epoch millis payload).

**Call path**

- canonicalPayload hashed with Hashes.sha256Hex

This folder is documentation only. It does not change the Java class.
