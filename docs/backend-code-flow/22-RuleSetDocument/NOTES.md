# com.fairhome.rules.RuleSetDocument

**File:** `src/main/java/com/fairhome/rules/RuleSetDocument.java`

Parsed JSON rule book: categories, reservations, draw seed, spill, waitlist, dedup cfg.

**Call path**

- RuleSetService.parse -> engine + intake + status

This folder is documentation only. It does not change the Java class.
