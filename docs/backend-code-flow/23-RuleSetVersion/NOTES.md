# com.fairhome.rules.RuleSetVersion

**File:** `src/main/java/com/fairhome/rules/RuleSetVersion.java`

Versioned stored JSON + content hash + active flag.

**Call path**

- publish writes a new row; draws record version+hash

This folder is documentation only. It does not change the Java class.
