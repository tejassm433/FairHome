# com.fairhome.config.FairHomeProperties

**File:** `src/main/java/com/fairhome/config/FairHomeProperties.java`

Binds fairhome.* from application.properties (admin login, demo seed flags).

**Call path**

- application.properties -> FairHomeProperties -> SecurityConfig / CurrentOfficer / DemoDataSeeder

This folder is documentation only. It does not change the Java class.
