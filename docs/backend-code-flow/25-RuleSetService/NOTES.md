# com.fairhome.rules.RuleSetService

**File:** `src/main/java/com/fairhome/rules/RuleSetService.java`

Load, validate, canonicalise, hash, publish. Bootstraps shipped default.

**Call path**

- PUT /api/admin/rules -> publish; GET /api/rules -> activeVersion + parse

This folder is documentation only. It does not change the Java class.
