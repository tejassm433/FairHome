# com.fairhome.application.ApplicationForm

**File:** `src/main/java/com/fairhome/application/ApplicationForm.java`

JSON/HTML input DTO. Public and offline share it; paper fields only on admin.

**Call path**

- Postman JSON -> @Valid ApplicationForm -> IntakeService

This folder is documentation only. It does not change the Java class.
