# com.fairhome.dedup.DuplicateFlag

**File:** `src/main/java/com/fairhome/dedup/DuplicateFlag.java`

Stored suspicion: new vs existing, match type, score, resolution.

**Call path**

- raiseFlags saves OPEN; resolve updates resolution

This folder is documentation only. It does not change the Java class.
