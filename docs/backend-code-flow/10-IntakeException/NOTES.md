# com.fairhome.application.IntakeException

**File:** `src/main/java/com/fairhome/application/IntakeException.java`

Field errors that become HTTP 422 via ApiExceptionHandler.

**Call path**

- IntakeService throws -> ApiExceptionHandler.intake

This folder is documentation only. It does not change the Java class.
