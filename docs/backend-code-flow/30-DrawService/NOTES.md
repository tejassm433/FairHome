# com.fairhome.draw.DrawService

**File:** `src/main/java/com/fairhome/draw/DrawService.java`

Load applicants, refuse final if queue open, persist DrawRun + Allocation rows, publish.

**Call path**

- POST /api/admin/draws -> run; POST .../publish -> publish

This folder is documentation only. It does not change the Java class.
