# com.fairhome.FairhomeApplication

**File:** `src/main/java/com/fairhome/FairhomeApplication.java`

Boots Spring Boot, loads FairHomeProperties, starts Tomcat on 8080.

**Call path**

- main() -> SpringApplication.run
- DemoDataSeeder runs after context is ready

This folder is documentation only. It does not change the Java class.
