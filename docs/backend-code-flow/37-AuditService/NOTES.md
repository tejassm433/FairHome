# com.fairhome.audit.AuditService

**File:** `src/main/java/com/fairhome/audit/AuditService.java`

Append hash-chained events; verifyChain; repair unstable Instant hashes on boot.

**Call path**

- Almost every write calls record(); GET /api/admin/audit/verify

This folder is documentation only. It does not change the Java class.
