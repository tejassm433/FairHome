# com.fairhome.web.ApiController

**File:** `src/main/java/com/fairhome/web/ApiController.java`

JSON facade /api/** used by Postman. Same services as the HTML screens.

**Call path**

- See docs/postman-scenarios

This folder is documentation only. It does not change the Java class.
