# com.fairhome.seed.DemoDataSeeder

**File:** `src/main/java/com/fairhome/seed/DemoDataSeeder.java`

First-start generator through IntakeService (~4000 apps).

**Call path**

- ApplicationRunner after boot if fairhome.demo-data.enabled=true

This folder is documentation only. It does not change the Java class.
