# com.fairhome.draw.Allocation

**File:** `src/main/java/com/fairhome/draw/Allocation.java`

Per-application decision in a run: outcome, ranks, seat, token, explanation.

**Call path**

- GET status after publish reads this

This folder is documentation only. It does not change the Java class.
