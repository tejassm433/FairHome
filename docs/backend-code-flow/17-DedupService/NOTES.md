# com.fairhome.dedup.DedupService

**File:** `src/main/java/com/fairhome/dedup/DedupService.java`

findCandidatesAgainst + raiseFlags + resolve. Never auto-rejects.

**Call path**

- IntakeService.findCandidates -> raiseFlags; ApiController.resolve -> resolve

This folder is documentation only. It does not change the Java class.
